{
  "name": "human-bridges",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node --check server.js && node --check public/app.js",
    "lint": "npm run build",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2"
  }
}
